{
  "name": "helix-portfolio",
  "private": true,
  "scripts": {
    "dev": "cd backend && .venv/bin/uvicorn main:app --reload --port 8000",
    "build": "echo 'No build step needed for Python + Jinja2'"
  }
}
